#include<bits/stdc++.h>
using namespace std;
int solve(vector<int>& x,int l,int r,int sum){
    int sum1=0,sum2=0;
    while(l<r){
        sum1+=x[l];
        sum2+=x[r];
        if(sum1==sum2){
            
        }
    }
}
int main() {
ios::sync_with_stdio(0);
cin.tie(0);
int t;cin >> t;
while (t--) {
    int n;
    cin >> n;
    vector<int>v;
    for(int i=0;i<n;i++){
    int element;
    cin>>element;
    v.push_back(element);
    }
    
}
return 0;
}