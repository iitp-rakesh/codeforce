#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    long long t;
    cin >> t;
    while (t--)
    {
        long long n;
        cin >> n;
        long long a[n];
        int mp[10e9];
        for (long long i = 0; i < n; i++)
        {
            cin >> a[i];
            mp[a[i]]++;
        }
        sort(a,a+n);
    while(n--){
       while(a[i]==a[i+1]){
        
       }
    }
        cout << endl;
    }
    return 0;
}
WHEN '2221CS01' THEN 'ravi.shah@yahoo.com'
        WHEN '2221CS02' THEN 'neha.patel@gmail.com'
        WHEN '2221CS03' THEN 'anil.kumar@iitp.ac.in'
        WHEN '2221CS04' THEN 'soniya.singh@iitp.ac.in'
        WHEN '2221CS05' THEN 'vikas.gupta@iitp.ac.in'
        WHEN '2221CS06' THEN 'priya.kumar@gmail.com'
        WHEN '2221CS07' THEN 'amit.singh@yahoo.com'
        WHEN '2221CS08' THEN 'megha.patel@domain.com'
        WHEN '2221CS09' THEN 'rajesh.shah@domain.com'
        WHEN '2221CS10' THEN 'nita.singh@iitp.ac.in